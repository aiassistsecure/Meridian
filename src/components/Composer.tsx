import { FormEvent, useState } from 'react'
export function Composer({ onSend }: { onSend: (text: string) => void }) { const [text, setText] = useState(''); function send(event: FormEvent) { event.preventDefault(); const value = text.trim(); if (!value) return; onSend(value); setText('') } return <form className="composer" onSubmit={send}><div className="compose-box"><input value={text} onChange={(event) => setText(event.target.value)} placeholder="Write to the room…" aria-label="Message room" autoComplete="off" /><button type="submit">Send</button></div><div className="composer-state"><span>sealed locally before relay</span><span>⌘ ↵ to send</span></div></form> }
