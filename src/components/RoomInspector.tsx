type Props = { downloadAll: boolean; onToggle: () => void; peers: number }
export function RoomInspector({ downloadAll, onToggle, peers }: Props) { return <aside className="inspector" aria-label="Room state"><p className="inspector-title">Room state</p><section className="retention"><div><strong>24h</strong><span>full content</span></div><p>After expiry, this replica keeps only the signed receipt and checkpoint proof.</p></section><section><InspectorLabel label="Local replica" value="synced" /><StateRow label="causal DAG" value="218 objects" /><StateRow label="session root" value="verified" /></section><section><InspectorLabel label="Peers" value={`${peers} online`} /><StateRow label="vex" value="live" /><StateRow label="nova" value="live" /><StateRow label="iris" value="live" /></section><section><div className="history-toggle"><span>Download all on entry</span><button className={downloadAll ? 'toggle enabled' : 'toggle'} type="button" aria-label="Download all history on entry" aria-pressed={downloadAll} onClick={onToggle}><span /></button></div><p className="history-note">{downloadAll ? 'On. Requesting retained history from connected peers.' : 'Off. You joined at the current verified frontier.'}</p><p className="history-status"><b>{downloadAll ? 'Availability is best-effort.' : 'No history requested.'}</b></p></section><p className="network-note">No host. No canonical writer. Your copy is real.</p></aside> }
function InspectorLabel({ label, value }: { label: string; value: string }) { return <div className="inspector-label"><span>{label}</span><span>{value}</span></div> }
function StateRow({ label, value }: { label: string; value: string }) { return <div className="state-row"><span><i />{label}</span><small>{value}</small></div> }
